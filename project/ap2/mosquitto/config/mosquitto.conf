# Mosquitto broker configuration for the Brauanlage Digital Twin (demo).
# Anonymous access is enabled to keep the university demo simple; secure this
# with authentication for any real deployment.
listener 1883
allow_anonymous true

# Keep logs on stdout so `docker compose logs mosquitto` shows activity.
log_dest stdout
log_type all
