# AP5 simulation (shared module)

See `project/shared/simulation/simulation_contract.md` for the full contract.

AP5 re-exports the shared `ProcessSimulator` so integration tests and docs can
reference the same implementation used by AP2 and AP3.
